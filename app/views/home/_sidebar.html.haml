%h2
  %span= _("Recipes")
- unless @categories.blank?
  %ul
    - @categories.each do |category|
      %li
        = link_to category.name, category

- unless @users.empty?
  .users
    %h2= _("Featured users")
    %ul.featured-users
      - @users.each do |user|
        %li.user
          = link_to image_tag(user.avatar.url(:medium), :class => "avatar", :alt => "", :width => "40px"), user
          %p= link_to user.name, user
          %p.meta
            = _("%{num} recipes") % {:num => user.recipes.size}
            %span.ft= '|'
            = link_to n_("%{num} follower", "%{num} followers", user.followers.size) % {:num => user.followers.size}

